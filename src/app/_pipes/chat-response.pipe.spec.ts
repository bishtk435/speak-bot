import { ChatResponsePipe } from './chat-response.pipe';

describe('ChatResponsePipe', () => {
  it('create an instance', () => {
    const pipe = new ChatResponsePipe();
    expect(pipe).toBeTruthy();
  });
});
